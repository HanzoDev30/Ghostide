package ir.hanzodev1375.components.store.event;

/** Posted after a theme is downloaded and applied so the launcher activity can rebuild. */
public class ThemeInstalledEvent {

  public ThemeInstalledEvent() {}
}
